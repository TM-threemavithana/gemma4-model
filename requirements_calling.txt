# Asterisk Calling Agent — dependencies
# Run: pip install -r requirements_calling.txt

# STT
# faster-whisper  (installed separately — needs special build)

# TTS — Piper runs 100% locally, no internet needed (~50ms vs 2-4s for gTTS)
piper-tts

# Async HTTP client for Gemma 4 requests
httpx>=0.27.0

# Optional but recommended for better voice activity detection:
# webrtcvad-wheels>=2.0.14   # uncomment for production-grade VAD
