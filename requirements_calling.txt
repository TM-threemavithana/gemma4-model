# Asterisk Calling Agent — extra dependencies
# Run: pip install -r requirements_calling.txt

# Already installed (from your Jupyter notebook):
# faster-whisper
# gTTS
# pydub

# New additions:
httpx>=0.27.0          # async HTTP client for Gemma 4 requests
pydub>=0.25.1          # audio format conversion (MP3→PCM)


# Optional but recommended for better voice activity detection:
# webrtcvad-wheels>=2.0.14   # uncomment for production-grade VAD
